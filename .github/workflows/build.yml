name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - master

env:
  DOTNET_VERSION: "10.x"

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: src/NexaERP

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore NexaERP.sln

      - name: Build
        run: dotnet build NexaERP.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test NexaERP.sln --configuration Release --no-restore --no-build --verbosity normal

      - name: Publish
        run: dotnet publish NexaERP.sln --configuration Release --no-restore --no-build
